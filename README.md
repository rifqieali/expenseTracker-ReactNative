# ExpenseTracker React Native

Personal expense tracker lokal (offline-first), UI terinspirasi Jago.
Stack: Expo + TypeScript + Expo Router + Drizzle ORM + expo-sqlite.

## Status
v1 spec: lihat GitHub Issues `ready-for-agent` + `docs/specs/`.
