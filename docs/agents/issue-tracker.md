# Issue tracker

GitHub Issues di `rifqieali/expenseTracker-ReactNative` adalah tracker utama.
Gunakan `gh` CLI: `gh issue create`, `gh issue list`, `gh issue view`.
PRs are NOT part of the triage queue.
