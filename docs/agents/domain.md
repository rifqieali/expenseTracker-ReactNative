# Domain docs

Single-context. `CONTEXT.md` di root adalah glosarium domain.
`docs/adr/` untuk Architecture Decision Records.

Vocabulary: Pocket (kantong ala Jago), Transaction (expense/income),
Budget (batas per pocket), Dashboard (ringkasan home).
